{
  "name": "chip8-interpreter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://saxolandon.github.io/chip8-interpreter",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "typescript": "^4.6.4",
    "vite": "^3.2.3"
  }
}
